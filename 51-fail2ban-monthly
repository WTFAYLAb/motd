#!/bin/env bash

## Should check if these exist before adding them to the array
LogFile=/opt/var/log/fail2ban.log
Logfiles=()
[[ -f $LogFile ]] && Logfiles+=("$LogFile")
[[ -f ${LogFile}.1 ]] && Logfiles+=("${LogFile}.1")
mapfile -t Lines < <(
	grep -ahioP '(\[[a-z-]+\]) ?(?:restore)? (ban|unban)' "${Logfiles[@]}" \
	| sort \
	| uniq -c
)
mapfile -t Jails < <(
	printf -- '%s\n' "${Lines[@]}" \
	| grep -oP '\[\K[^\]]+' \
	| sort -u
)

Out=
for Jail in "${Jails[@]}"; do
	Bans=$(
		printf -- '%s\n' "${Lines[@]}" \
		| grep -iP "[[:digit:]]+ \[$Jail\] ban" \
		| awk '{print $1}'
	)
	Restores=$(
		printf -- '%s\n' "${Lines[@]}" \
		| grep -iP "[[:digit:]]+ \[$Jail\] restore ban" \
		| awk '{print $1}'
	)
	Unbans=$(
		printf -- '%s\n' "${Lines[@]}" \
		| grep -iP "[[:digit:]]+ \[$Jail\] unban" \
		| awk '{print $1}'
	)
	Bans=${Bans:-0} # default value
	Restores=${Restores:-0} # default value
	Unbans=${Unbans:-0} # default value
	Bans=$(( Bans + Restores ))
	Diff=$(( Bans - Unbans))
	[[ $Diff -lt 0 ]] && Diff=0
	Out+=$(printf "${Jail}, %+3s bans, %+3s unbans, %+3s active" ${Bans} "${Unbans}" ${Diff})"\n"
done

printf "\nfail2ban status (monthly):\n"
# shellcheck disable=SC2059
printf "$Out" \
| column -ts $',' \
| sed -e 's/^/  /'